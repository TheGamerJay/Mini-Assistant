"""
core/ — CEO Router package.

Entry point: core.ceo_router.route_request(RouterRequest) -> RouterDecision

Do not import individual sub-modules directly in application code.
Always go through route_request().
"""
