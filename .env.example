# Core
SECRET_KEY=change-me
DATABASE_URL=postgresql://username:password@host:port/database_name
# DATABASE_URL=sqlite:///mcw.db  # Fallback for local development
APP_BASE_URL=https://your-domain.example

# Mail (pick ONE path; echo for dev)
DEV_MAIL_ECHO=true

# Resend (preferred)
# RESEND_API_KEY=...
# RESEND_FROM=Mini Casino World <no-reply@yourdomain>

# SMTP (fallback)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-user
# SMTP_PASS=your-password
# SMTP_FROM=Mini Casino World <no-reply@yourdomain>
# SMTP_USE_TLS=true

PASSWORD_RESET_TOKEN_MAX_AGE=3600
PREFERRED_URL_SCHEME=https
ASSET_VERSION=v1